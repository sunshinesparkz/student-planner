# ตัวอย่าง Environment Variables สำหรับโปรเจกต์ Vite
# เวลาใช้งานจริง ให้สร้างไฟล์ .env และคัดลอกค่าเหล่านี้ไปใส่ (ไม่ต้อง commit ไฟล์ .env จริง)

# ชื่อแอปพลิเคชัน
VITE_APP_TITLE=Student Planner

# การตั้งค่า Supabase (สำหรับ Cloud Database)
# สมัครที่ https://supabase.com -> Create Project -> Settings -> API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here